services:
  - type: web
    name: poker-ai-api
    env: python
    buildCommand: "pip install -r requirements.txt && python cfr_trainer.py"
    startCommand: "uvicorn fastapi_backend:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16
