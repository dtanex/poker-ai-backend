services:
  - type: web
    name: poker-ai-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn fastapi_backend_v3_prod:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
