services:
  - type: web
    name: poker-ai-api
    env: python
    buildCommand: "pip install -r requirements.txt && python -c 'from cfr_trainer_pro import ProfessionalCFRTrainer; t = ProfessionalCFRTrainer(); t.train_preflop(10000); t.train_postflop(5000); t.save_strategies(\"gto_strategies.pkl\"); print(\"✅ Professional strategies trained\")'"
    startCommand: "uvicorn fastapi_backend:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
